name: manage workflows

on:
  push:
  pull_request:
    branches:
      - main
      - develop
    
jobs:
  first:
    name: pretend the First workflow
    uses: kirbyAlasdairAnswer/AnswerKing-CS/.github/workflows/build-and-test.yml@${{ github.sha }}}

  second:
    name: pretend the Second workflow
    needs: first
    if: github.event_name == 'pull_request'
    uses: kirbyAlasdairAnswer/AnswerKing-CS/.github/workflows/report-and-analyze.yml@${{ github.sha }}}
    secrets: inherit
